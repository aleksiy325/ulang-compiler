int sampleOne (int x, float y)
{
    a[a[a[a[1]]]];
    if (a*b*c*a+b-c<a<b<c==a==b==c)
    {
        print "Hello";
    }
    else
    {
        print "Goodbye";
    }
    return x*y;
}

void sampleTwo (int x)
{
    float[10] b;
    while (x<10)
    {
        x=x-1;
        while (x<10)
        {
            x=x-1;
            if (a*b*c*a+b-c<a<b<c==a==b==c)
            {   
                print "Hello";
            }
            else
            {
                print "Goodbye";
            }
        }
    }
    x=8;
    return a+b*2==1-2<3<4+2+b[2]+f(test,f(test),f(test));
}

void main ()
{
    string[10] a;
    int w;
    println sampleOne(10,9.3,false,true,'c',"string");
    sampleTwo();
}